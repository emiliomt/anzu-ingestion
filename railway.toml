[build]
builder = "NIXPACKS"

[deploy]
startCommand = "echo '=== ANZU DEBUG ===' && echo PORT=$PORT && ls -la /app/server.js && node --version && node server.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
