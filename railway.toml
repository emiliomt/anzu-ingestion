[build]
builder = "NIXPACKS"

[deploy]
startCommand = "./node_modules/.bin/prisma db push --skip-generate && node server.js"
healthcheckPath = "/api/metrics"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
