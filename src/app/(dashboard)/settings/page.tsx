// Anzu Dynamics — Tenant Settings / ERP Credential Vault (placeholder)
// Full implementation in Step 3.
// Allows Admins to add/update/delete encrypted ERP credentials per tenant.

export default function SettingsPage() {
  return (
    <div className="p-8">
      <h1 className="text-xl font-bold text-gray-900 mb-2">Settings</h1>
      <p className="text-gray-500 text-sm">ERP credential vault implemented in Step 3.</p>
    </div>
  );
}
